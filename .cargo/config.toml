# macOS aarch64 link flags required by Rust/LEZ native dependencies.
[target.'cfg(target_os = "macos")']
rustflags = [
    "-C", "link-args=-framework CoreFoundation -framework Security -framework CoreServices -lresolv",
]

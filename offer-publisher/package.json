{
  "name": "offer-publisher",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Headless maker offer-publisher sidecar for the LEZ<->ETH liquidity bot (swap-cli maker --loop). Republishes the offer to the logos.dev fleet (cluster 2, TCP) because the fleet runs store=false.",
  "scripts": {
    "smoke": "node smoke.mjs",
    "publish-offer": "node publish-offer.mjs"
  },
  "dependencies": {
    "@libp2p/tcp": "^10.1.19",
    "@multiformats/multiaddr": "^12.5.1",
    "@waku/sdk": "^0.0.36",
    "@waku/utils": "^0.0.27"
  }
}

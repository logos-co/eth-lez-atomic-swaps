# Local Rust FFI binaries — produced by `make swap-vendor-ffi` for ad hoc
# non-Nix testing. Nix builds compile swap-ffi from tracked Rust source and
# stage the resulting library into lib/ inside the build sandbox.
libswap_ffi.dylib
libswap_ffi.so
libswap_ffi.dll
